{
  "name": "hanhan",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "find src test -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
